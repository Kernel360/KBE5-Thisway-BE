route:
  receiver: local-evidence
  group_by: [alertname]
  group_wait: 1s
  group_interval: 1s
  repeat_interval: 1h
receivers:
  - name: local-evidence
    webhook_configs:
      - url: http://receiver:8080/events
        send_resolved: true
